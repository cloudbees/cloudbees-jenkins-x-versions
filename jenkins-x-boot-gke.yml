buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: gcr.io/jenkinsxio/builder-go:0.0.0-SNAPSHOT-PR-4819-15
        stages:
          - name: ci
            steps:
              - command: jx/bdd/boot-gke/ci.sh
                name: runci
